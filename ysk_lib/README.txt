ysk_lib — Numerical Integration (C++17)

Build (Linux/macOS):
  mkdir -p build
  cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
  cmake --build build -j

Run:
  ./build/main     # 1D tests
  ./build/main2D   # 2D extension

Notes:
- This archive contains only source files (no build artifacts).
- The PDF report is provided separately, as requested by the assignment instructions.
